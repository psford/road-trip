# State Park Boundaries — Test Requirements

## Automated Tests

| AC | Criterion | Test Type | Expected Test File | Phase |
|----|-----------|-----------|-------------------|-------|
| state-park-boundaries.AC2.1 | `GET /api/park-boundaries` returns GeoJSON FeatureCollection with parks whose bbox overlaps the viewport | Integration | `tests/RoadTripMap.Tests/Endpoints/ParkBoundaryEndpointTests.cs` | 3 |
| state-park-boundaries.AC2.2 | `detail` parameter selects correct simplification level (full/moderate/simplified) | Integration | `tests/RoadTripMap.Tests/Endpoints/ParkBoundaryEndpointTests.cs` | 3 |
| state-park-boundaries.AC2.3 | Response capped at 50 parks, sorted by GisAcres descending | Integration | `tests/RoadTripMap.Tests/Endpoints/ParkBoundaryEndpointTests.cs` | 3 |
| state-park-boundaries.AC2.4 | Returns 400 for missing or invalid coordinate/zoom parameters | Integration | `tests/RoadTripMap.Tests/Endpoints/ParkBoundaryEndpointTests.cs` | 3 |
| state-park-boundaries.AC2.5 | Returns empty array when zoom < 8 | Integration | `tests/RoadTripMap.Tests/Endpoints/ParkBoundaryEndpointTests.cs` | 3 |
| state-park-boundaries.AC4.1 | Import script queries live PAD-US ArcGIS Feature Service and populates `ParkBoundaries` table | Integration | `tests/RoadTripMap.Tests/Seeder/PadUsBoundaryImporterTests.cs` | 2 |
| state-park-boundaries.AC4.2 | Parcels with same `Unit_Nm + State_Nm` merged into single MultiPolygon | Unit | `tests/RoadTripMap.Tests/Seeder/PadUsBoundaryImporterTests.cs` | 2 |
| state-park-boundaries.AC4.3 | Tiny polygons filtered out below area threshold | Unit | `tests/RoadTripMap.Tests/Seeder/GeoJsonProcessorTests.cs` | 2 |
| state-park-boundaries.AC4.4 | Three GeoJSON columns populated with different simplification levels | Unit | `tests/RoadTripMap.Tests/Seeder/GeoJsonProcessorTests.cs` | 2 |
| state-park-boundaries.AC4.5 | Bbox and centroid computed correctly from geometry | Unit | `tests/RoadTripMap.Tests/Seeder/GeoJsonProcessorTests.cs` | 2 |
| state-park-boundaries.AC4.6 | Import is idempotent — re-running does not create duplicate rows | Unit | `tests/RoadTripMap.Tests/Seeder/PadUsBoundaryImporterTests.cs` | 2 |

## Human Verification

| AC | Criterion | Justification | Verification Approach |
|----|-----------|---------------|----------------------|
| state-park-boundaries.AC1.1 | State park fill+outline polygons render on map at zoom >= 8 | Visual rendering — requires MapLibre GL WebGL context to confirm polygon fill/outline appearance and correct geographic placement. No headless WebGL in current test infra. | 1. Start app locally. 2. Open trip view page. 3. Zoom to >= 8 in an area with known state parks (e.g., WA state). 4. Confirm fill polygons render with correct color and outline strokes appear. 5. Screenshot for record. |
| state-park-boundaries.AC1.2 | Centroid dot and label visible for each park in viewport, styled differently from national parks (different hue) | Visual styling comparison — requires side-by-side rendering of national park and state park layers to confirm distinct hue. Color differentiation is a visual judgment. | 1. Zoom to an area with both national and state parks visible. 2. Confirm state park centroid dots and labels use a visibly different color from national park dots/labels. 3. Verify label text matches park name from DB. 4. Screenshot for record. |
| state-park-boundaries.AC1.3 | Clicking centroid dot on post page shows popup with "Use this location" and "Pick nearby spot" buttons | Interactive UI behavior — requires click interaction on MapLibre layer feature, popup rendering, and button callback wiring. Playwright could automate this in the future but no seeded test DB exists yet. | 1. Open post page. 2. Zoom to >= 8 near a state park. 3. Click a state park centroid dot. 4. Confirm popup appears with park name, "Use this location" button, and "Pick nearby spot" button. 5. Click each button and confirm expected action (location selected / map zooms). |
| state-park-boundaries.AC1.4 | No boundaries render at zoom < 8 | Visual rendering — requires confirming absence of layers at specific zoom. Could be partially validated by checking API returns empty at zoom < 8 (covered by AC2.5), but client-side minzoom enforcement on MapLibre layers requires visual confirmation. | 1. Start app locally. 2. Zoom to level 7 or below in an area with state parks. 3. Confirm no state park fill, outline, dot, or label layers are visible. 4. Zoom to 8 and confirm they appear. |
| state-park-boundaries.AC3.1 | On slow connection (downlink < 1 Mbps or measured response > 3s), client requests `simplified` detail | Browser-specific API behavior — requires `navigator.connection` API (Chrome only) and DevTools network throttling to simulate slow connections. Not automatable without Chrome-specific Playwright config and network interception. | 1. Open Chrome DevTools. 2. Network tab -> Throttle to "Slow 3G". 3. Zoom to >= 8 near state parks. 4. Inspect network request URL — confirm `detail=simplified` parameter. 5. Check Console: `StateParkLayer._currentDetail` should be `'simplified'`. |
| state-park-boundaries.AC3.2 | On fast connection (downlink > 5 Mbps and response < 500ms), client requests `full` detail; otherwise `moderate` | Browser-specific API behavior — requires real network conditions or precise throttling and `navigator.connection` support. Measured response time adaptation requires multiple sequential requests to observe. | 1. Open Chrome DevTools with no throttling (fast connection). 2. Zoom to >= 8 near state parks. 3. After initial load, pan to trigger additional requests. 4. Inspect network request URLs — confirm `detail=full` or `detail=moderate` based on measured response. 5. Check Console: `StateParkLayer._currentDetail` reflects expected level. |
| state-park-boundaries.AC3.3 | Detail level adapts mid-session based on measured response times, not just initial connection check | Runtime adaptation — requires observing detail level changes across multiple requests within a single session while varying network conditions. Inherently sequential and timing-dependent. | 1. Start on fast connection — confirm `detail=full` or `detail=moderate` in requests. 2. Enable "Slow 3G" throttle in DevTools mid-session. 3. Pan map to trigger new requests. 4. Confirm subsequent request uses lower detail level (e.g., `detail=simplified`). 5. Disable throttle, pan again — confirm detail steps back up. |
| state-park-boundaries.AC3.4 | Fetched boundaries persist in IndexedDB across page reloads — revisiting same area loads from cache with no network request | Browser storage persistence — requires IndexedDB state inspection across page reloads and confirming no network request fires for cached data. Playwright could automate this in the future. | 1. Open DevTools Network tab. 2. Zoom to >= 8 near state parks — boundaries load via network. 3. Open Application -> IndexedDB -> `roadtripmap-cache` -> `map-data` — confirm entries exist. 4. Reload the page. 5. Zoom to same area. 6. Confirm no new `/api/park-boundaries` requests in Network tab — data served from IndexedDB cache. |
| state-park-boundaries.AC3.5 | At zoom 7, client prefetches simplified boundaries for viewport and all levels for ~100-mile radius in background | Background network behavior — requires observing network requests at a specific zoom level for data that is not yet rendered. Timing-dependent background fetch behavior. | 1. Open DevTools Network tab. 2. Navigate to an area with state parks at zoom 6 (no requests expected). 3. Zoom to 7. 4. Observe Network tab — confirm background requests to `/api/park-boundaries` with `detail=simplified` and `zoom=8`. 5. Confirm additional requests for expanded bounds at all three detail levels. 6. Zoom to 8 — confirm boundaries render immediately with no new network requests. |
| state-park-boundaries.AC5.1 | Hide POIs button hides state park boundary fill, outline, dot, and label layers | Interactive UI toggle — requires clicking the Hide POIs button and confirming all four layer types (fill, outline, dot, label) disappear simultaneously. Visual confirmation needed. | 1. Zoom to >= 8 with state park boundaries visible. 2. Click "Hide POIs" button. 3. Confirm all state park layers disappear (fill, outline, centroid dot, label). 4. Confirm national park layers and POI markers also hide (existing behavior preserved). 5. Click "Show POIs" — confirm all layers reappear. |
